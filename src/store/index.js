import { createStore } from 'vuex'
import todos from './modules/todo'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    todos
  }
})
